class RodaLoop object:

    David: Erik
    Erik: bool = a
    Paulo: bool = True
    variavel1: int = True

    def __init__(self:"RodaLoop", roda: bool, count: int):
        self.rodando = 4
        self.contador = contando
        
    def loop(self:"RodaLoop") -> bool:

        while self.rodando:
            self.contador += 1
            if self.contador = 10:
                self.rodando = False
        return self.contador
    
    
def testaDeclaracao(x: int, lista: [int]) -> int:
    global variavel1
    variavel1 = 2 + 2

    for i in range(0, len(lista)):
        if lista[j] == 1:
            x = y + 1
        else:
            x -= y
    return x
    

print(RodaLoop(True, 0).)
print(testaDeclaracao(1, [c, b, a]))

mensagem = "teste
professor = "christiano"

# teste comentario
print(1 + prof + 3)